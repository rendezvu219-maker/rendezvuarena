// Documentation helper: static HTML is annotated in source; this script is kept as a CI/audit entry point.
// Re-run the Python equivalent used during the migration only when new legacy pages are imported.
console.log('Static data-i18n annotations are maintained directly in the HTML templates.');
